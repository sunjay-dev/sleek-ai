{
  "name": "gen-ai",
  "scripts": {
    "lint-all": "bun --filter ./backend lint && bun --filter ./frontend lint",
    "prepare": "husky"
  },
  "author": "https://github.com/sunjay-dev/",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
